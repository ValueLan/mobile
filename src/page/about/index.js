import React, { Component } from 'react';
import classList from './index.module.scss';

export default class extends Component {
  render() {
    return <div className={classList.main}>about</div>
  }
}